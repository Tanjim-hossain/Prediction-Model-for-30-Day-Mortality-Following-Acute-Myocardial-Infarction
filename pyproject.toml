[project]
name = "ami-30day-mortality-prediction"
version = "1.1.0"
description = "Internally validated 30-day mortality prediction after acute myocardial infarction."
requires-python = ">=3.12"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
